import { RequestToProyecto } from './request-to-proyecto.model';

describe('RequestToProyecto', () => {
  it('should create an instance', () => {
    expect(new RequestToProyecto()).toBeTruthy();
  });
});
