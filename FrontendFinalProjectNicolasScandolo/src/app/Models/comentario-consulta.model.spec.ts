import { ComentarioConsulta } from './comentario-consulta.model';

describe('ComentarioConsulta', () => {
  it('should create an instance', () => {
    expect(new ComentarioConsulta()).toBeTruthy();
  });
});
